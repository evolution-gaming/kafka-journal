include "application.conf"

akka {
  loglevel = "WARNING"

  test {
    timefactor = 3.0
  }
}

kafka.persistence.journal {
  kafka {
    producer {
      acks = "1"
      enable-idempotence = false
      retries = 0
    }
  }
}