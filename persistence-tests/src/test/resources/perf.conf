include "application.conf"

akka {
  loglevel = "WARNING"

  test {
    timefactor = 3.0
  }
}

kafka.persistence.journal {

  persistence-id-to-key.constant-topic.topic = "perf"

  kafka {
    producer {
      acks = "1"
      enable-idempotence = false
      retries = 0
    }
  }
}