include "application.conf"


kafka.persistence.journal {

//  persistence-id-to-key.constant-topic.topic = "consistency"

  kafka {
    producer {
      acks = "all"
      enable-idempotence = true
      retries = 5
    }

    consumer {
      max-poll-records = 10
    }
  }
}